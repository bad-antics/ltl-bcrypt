// examples/demo.ltl — runnable demo for ltl_bcrypt

import std::io
use ltl_bcrypt as lib

fn main() {
    io::println("ltl_bcrypt demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}