// ltl-bcrypt/main.ltl
//
// bcrypt password-hashing with cost selection.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-bcrypt v0.1.0")
    io::println("bcrypt password-hashing with cost selection.")
    0
}
